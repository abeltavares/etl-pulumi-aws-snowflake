name: aws_etl_pipeline
runtime:
  name: python
  options:
    virtualenv: venv
description: A Pulumi program that deploys an AWS Glue ETL job that transforms and loads data from S3 to Snowflake
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
